// This file is no longer the source of truth — it lived under Astro's
// content collections and used the `astro:content` virtual import. The
// Next.js port reads markdown directly via lib/content.ts using
// gray-matter, and the runtime types live in lib/content.ts as well.
// Keeping the file for historical reference only; it is not imported.
//
// See lib/content.ts for the actual book/toolkit loader + types.
export {};
